= S3 =

== CLI ==
    * copy files
        * `aws s3 cp destination target`
    * copy files with wildcards
        * `aws s3 cp destination target --recursive --exclude "*" --include "filter*"`
            * all flags are required
            * order matters
            * if you don't explicitly exclude everything it will include everything
